# Copy this file to: run/config/pv-secrets.properties  (gitignored, never commit the real key)
# In a packaged game install it lives in:  <instance>/.minecraft/config/pv-secrets.properties
# Hypixel developer key — ~300 requests / 5 min, must be renewed every ~2 days.
hypixel_api_key=YOUR_KEY_HERE
